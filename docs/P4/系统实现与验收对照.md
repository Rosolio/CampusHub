# 系统实现与验收对照

## 1. 代码基线

本对照基于 `dev` 分支源码。

### 1.1 后端实现结构

- 认证与用户：`AuthController`、`UserController`
- 任务与订单：`TaskController`、`TaskService`
- 评论与互动：`TaskCommentController`
- 评价与信用：`TaskReviewController`
- 消息通知：`MessageController`
- 反馈与仲裁：`FeedbackController`
- 后台管理：`AdminController`、`AnnouncementController`
- 安全支撑：`UserVO`（防密码泄露）、`JwtUtil`（token type claim）、`JwtAuthenticationFilter`（角色鉴权）、`SecurityConfig`（`/admin/**` 需 ADMIN 权限）、`GlobalExceptionHandler`（403 支持与 SLF4J 日志）
- 安全加固（commit `62aa029`）：移除第三方登录、注册限频修复、token type 校验、密码哈希泄露修复、角色鉴权、管理员路径保护

### 1.2 前端实现结构

- 登录注册：`frontend/src/pages/Auth.vue`
- 首页与任务流：`HomeGateway.vue`、`Discovery.vue`、`RequestDetail.vue`
- 话题广场：`TopicSquare.vue`
- 发布页：`Publish.vue`
- 消息中心：`Messages.vue`
- 个人中心：`Profile.vue`
- 评价页：`TaskReviewPage.vue`
- 反馈页：`FeedbackPage.vue`
- 管理后台：`frontend/src/pages/admin/*`

## 2. P0 功能对照

| 验收项 | 代码依据 | 状态 |
|---|---|---|
| 用户注册、登录与身份认证 | `/auth/login`、`/auth/register`、`/auth/refresh`、JWT 过滤器 | 已完成 |
| 需求发布、列表查询、详情查看 | `TaskController` 的 `GET /tasks`、`GET /tasks/{id}`、`POST /tasks` | 已完成 |
| 接单、订单状态流转、订单历史 | `POST /tasks/{id}/accept`、`/complete`、`/cancel`、`/my`、`/my/accepted` | 已完成 |
| 数据库初始化、基础接口、统一错误处理 | `schema.sql`、`data.sql`、`GlobalExceptionHandler` | 已完成 |

## 3. P1 功能对照

| 功能 | 代码依据 | 状态 | 备注 |
|---|---|---|---|
| 评价与信用体系 | `TaskReviewController`、`TaskReviewService`、`UserPointRecord` | 已完成 | 测试覆盖双向评价、重复评价、低分扣分 |
| 消息通知 | `MessageController`、`MessageService` | 已完成 | 接单、完成、评论回复均有通知逻辑 |
| 社区话题互动 | `TaskCommentController`、`TopicSquare.vue` | 已完成 | 评论、回复、评论点赞已实现 |
| 反馈与仲裁 | `FeedbackController`、`AdminController` | 基本完成 | 提交与管理处理存在，仲裁策略仍可细化 |
| 管理后台 | `/admin/*` 路由与接口 | 基本完成 | 已形成可访问后台骨架 |

## 4. 已验证结果

### 4.1 后端

- `mvn test`：通过
- `mvn verify`：通过
- 测试总数：`46`（8 个测试类）
- 失败数：`0`
- 错误数：`0`
- JaCoCo 行覆盖率：`61.45%`

### 4.2 前端

- `npm run build`：通过
- `npm run lint`：通过

### 4.3 安全加固（commit `62aa029`）

- 移除第三方登录（`ThirdPartyLoginRequest` 已删除，`Auth.vue` 已清理）
- `UserVO` DTO 防止密码哈希泄露
- JWT token type claim（access/refresh 区分）
- `JwtAuthenticationFilter` 基于数据库角色鉴权
- `SecurityConfig` 保护 `/admin/**` 路径
- `GlobalExceptionHandler` 支持 403 响应与 SLF4J 日志
- 登录限频（5 次/15 分钟，Redis 实现）

### 4.3 代码质量

- `mvn -B checkstyle:check`：通过
- Checkstyle 违规数：`0`

## 5. 与 P4 验收标准的差距

| 验收标准 | 当前情况 | 结论 |
|---|---|---|
| P0 优先级功能全部可用 | 代码结构与测试表明已具备 | 基本满足 |
| P1 优先级功能基本可用 | 评价、消息、评论、后台存在 | 基本满足 |
| 系统可以完整演示核心业务流程 | 代码与页面齐备，README 含运行说明 | 基本满足 |
| 核心模块单元测试覆盖率 >= 60% | JaCoCo 行覆盖率 `61.45%`，已达到门槛 | 满足 |
| 集成测试覆盖至少 1 个正常流程和 2 个异常流程 | 现有后端测试已覆盖主流程与部分异常，且可通过 `verify` 统一执行 | 基本满足 |
| 已配置 CI/CD 自动执行依赖安装、检查、测试和构建 | GitLab CI + GitHub Actions 双 CI 已完整配置 checkstyle、测试、覆盖率、lint、build | 满足 |
| CI/CD 有最近一次运行结果记录 | 本地已等价验证，GitLab 页面运行记录仍需在平台侧补截图或留档 | 部分满足 |

## 6. 结论

从“系统功能是否已经做出来”的角度看，`dev` 分支已经具备 P4 主体成果。从“课程验收材料是否完整”的角度看，当前剩余缺口已经收敛为平台侧证据留档：

1. GitLab Pipeline 最近一次运行截图或文字记录
2. 如老师要求，可补一张覆盖率报告页面截图
